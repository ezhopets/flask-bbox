# flask-bbox

#### Start
pip install Flask

pip install cv2

pip install pdfminer

pip install pdf2image

pip install Werkzeug

set FLASK_APP=app

flask run

or

flask run --host=0.0.0.0

http://127.0.0.1:5000/
